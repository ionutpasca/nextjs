{
  "scripts": {
    "build": "next build",
    "dev": "next",
    "now-build": "next build",
    "export": "npm run build && next export"
  },
  "dependencies": {
    "next": "^8.0.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  }
}
